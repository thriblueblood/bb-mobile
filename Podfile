# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'bbmobile' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for bbmobile
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Kingfisher', '~> 6.0'
  


  target 'bbmobileTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'bbmobileUITests' do
    # Pods for testing
  end

end
